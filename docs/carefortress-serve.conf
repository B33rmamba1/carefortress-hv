# CareFortress - serve time to guest VM networks only
allow 10.10.1.0/24
allow 10.10.2.0/24
allow 10.10.3.0/24
# Do not use deny all - it prevents chrony from binding to serve
# Access control is handled by the allow directives above
